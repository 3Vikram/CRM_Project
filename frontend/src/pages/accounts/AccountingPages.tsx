import { useMemo, useState } from 'react'
import { Download, Plus, X } from 'lucide-react'
import { useAccounting } from '@/components/accounts/accounting-context'
import { CHART, BankPayment, PurchaseInvoice, PurchaseLine, Voucher, VoucherLine, accountType, exportCsv, id, invoiceTotals, ledgerEntries, money, outstanding, paymentStatus, profitLoss, today, trialBalance, validateInvoice, validateVoucher } from '@/lib/accounting'

export function JournalRegisterPage(){
  const {companyId,data,setData}=useAccounting()
  const [editing,setEditing]=useState<Voucher|null>(null)
  const [error,setError]=useState('')
  const create=()=>{setError('');setEditing({id:id(),companyId,number:`JR-${String(data.vouchers.length+1).padStart(4,'0')}`,invoiceNumber:'',date:today(),reference:'',narration:'',lines:[newVoucherLine('debit','Rent Expense'),newVoucherLine('credit','Cash')],status:'draft'})}
  const save=(post:boolean)=>{if(!editing)return;const err=validateVoucher(editing);if(post&&err){setError(err);return}const entry={...editing,status:post?'posted':editing.status};setData({...data,vouchers:data.vouchers.some(v=>v.id===entry.id)?data.vouchers.map(v=>v.id===entry.id?entry:v):[entry,...data.vouchers]});setEditing(null)}
  const updateLine=(index:number,changes:Partial<VoucherLine>)=>{if(!editing)return;setEditing({...editing,lines:editing.lines.map((line,lineIndex)=>lineIndex===index?{...line,...changes}:line)})}
  return <div className="space-y-6">
    <Header title="Journal Register" description="Record invoice-linked debit and credit entries by item or account." action={<button className={button} onClick={create}><Plus className="w-4"/>New Journal Entry</button>}/>
    <Table headers={['Journal No.','Invoice No.','Date','Items / Accounts','Reference','Debit','Credit','Status','Actions']}><>{data.vouchers.map(v=>{const debit=v.lines.reduce((sum,line)=>sum+line.debit,0),credit=v.lines.reduce((sum,line)=>sum+line.credit,0),items=[...new Set(v.lines.map(line=>line.account).filter(Boolean))].join(', ');return <tr key={v.id}><td>{v.number}</td><td>{v.invoiceNumber||'—'}</td><td>{v.date}</td><td>{items||'—'}</td><td>{v.reference||'—'}</td><td>{money(debit)}</td><td>{money(credit)}</td><td><Status value={v.status}/></td><td className="space-x-1">{v.status==='draft'&&<><button className={secondary} onClick={()=>{setError('');setEditing(v)}}>Edit</button><button className={secondary} onClick={()=>setData({...data,vouchers:data.vouchers.filter(x=>x.id!==v.id)})}>Delete</button></>}{v.status==='posted'&&<button className={secondary} onClick={()=>setData({...data,vouchers:data.vouchers.map(x=>x.id===v.id?{...x,status:'cancelled'}:x)})}>Cancel</button>}</td></tr>})}</></Table>
    {!data.vouchers.length&&<Empty text="Create a journal entry for rent, salaries, utilities, adjustments, and other debit or credit transactions."/>}
    {editing&&<Modal onClose={()=>setEditing(null)}><div><h2 className="text-2xl font-serif font-bold">Journal entry</h2><p className="mt-1 text-sm text-gray-500">Add items just like an invoice, then choose whether each amount is a debit or credit.</p><div className="mt-4 grid grid-cols-1 gap-3 md:grid-cols-3"><Field label="Journal number"><input className={input} value={editing.number} onChange={e=>setEditing({...editing,number:e.target.value})}/></Field><Field label="Invoice number"><input className={input} placeholder="e.g. INV-1042" value={editing.invoiceNumber||''} onChange={e=>setEditing({...editing,invoiceNumber:e.target.value})}/></Field><Field label="Journal date"><input type="date" className={input} value={editing.date} onChange={e=>setEditing({...editing,date:e.target.value})}/></Field><Field label="Reference"><input className={input} placeholder="Optional reference" value={editing.reference} onChange={e=>setEditing({...editing,reference:e.target.value})}/></Field></div><div className="mt-5 overflow-auto"><table className="w-full text-left text-sm"><thead><tr className="border-b text-xs uppercase text-gray-500"><th className="px-2 py-2">Item / account</th><th className="px-2 py-2">Description</th><th className="px-2 py-2">Debit / Credit</th><th className="px-2 py-2">Amount</th><th className="px-2 py-2"/></tr></thead><tbody>{editing.lines.map((line,index)=>{const amount=line.entryType==='credit'?line.credit:line.debit;return <tr className="border-b border-gray-100" key={line.id}><td className="p-2"><select className={input} value={line.account} onChange={e=>updateLine(index,{account:e.target.value})}><option value="">Select item</option>{CHART.map(account=><option key={account.name} value={account.name}>{account.name}</option>)}</select></td><td className="p-2"><input className={input} placeholder="Item details" value={line.description} onChange={e=>updateLine(index,{description:e.target.value})}/></td><td className="p-2"><select className={input} value={line.entryType} onChange={e=>{const entryType=e.target.value as 'debit'|'credit';updateLine(index,{entryType,debit:entryType==='debit'?amount:0,credit:entryType==='credit'?amount:0})}}><option value="debit">Debit</option><option value="credit">Credit</option></select></td><td className="p-2"><input type="number" min="0" step="0.01" className={input} placeholder="0.00" value={amount||''} onChange={e=>{const value=Number(e.target.value);updateLine(index,{debit:line.entryType==='debit'?value:0,credit:line.entryType==='credit'?value:0})}}/></td><td className="p-2"><button className={secondary} onClick={()=>setEditing({...editing,lines:editing.lines.filter((_,lineIndex)=>lineIndex!==index)})}>Remove</button></td></tr>})}</tbody></table></div><button className={`mt-3 ${secondary}`} onClick={()=>setEditing({...editing,lines:[...editing.lines,newVoucherLine()]})}><Plus className="mr-1 inline w-3"/>Add item</button><Field label="Notes / narration"><textarea className={input} value={editing.narration} onChange={e=>setEditing({...editing,narration:e.target.value})}/></Field><div className="mt-4 flex flex-wrap gap-5 rounded-lg bg-gray-50 p-3 text-sm font-medium"><span>Total debit {money(editing.lines.reduce((sum,line)=>sum+line.debit,0))}</span><span>Total credit {money(editing.lines.reduce((sum,line)=>sum+line.credit,0))}</span></div>{error&&<p className="mt-3 text-sm text-red-600">{error}</p>}<div className="mt-5 flex gap-2"><button className={secondary} onClick={()=>save(false)}>Save draft</button><button className={button} onClick={()=>save(true)}>Post journal</button></div></div></Modal>}
  </div>
}

const input='w-full rounded border border-gray-300 px-2 py-1.5 text-sm'; const button='inline-flex items-center gap-1 rounded-lg bg-gray-900 px-3 py-2 text-sm font-medium text-white hover:bg-gray-700'; const secondary='rounded border border-gray-300 px-2 py-1 text-sm hover:bg-gray-50';
function Header({title,description,action}:{title:string;description:string;action?:React.ReactNode}){return <div className="flex flex-wrap items-end justify-between gap-3"><div><h1 className="text-4xl font-serif font-bold text-gray-900">{title}</h1><p className="mt-1 text-sm text-gray-600">{description}</p></div>{action}</div>}
function Status({value}:{value:string}){const c=value==='posted'||value==='Paid'||value==='Cleared'?'bg-emerald-50 text-emerald-700':value==='cancelled'?'bg-red-50 text-red-700':'bg-amber-50 text-amber-700';return <span className={`rounded-full px-2 py-0.5 text-xs ${c}`}>{value}</span>}
function Modal({children,onClose}:{children:React.ReactNode;onClose:()=>void}){return <div className="fixed inset-0 z-50 overflow-auto bg-black/30 p-4"><div className="mx-auto my-8 max-w-5xl rounded-lg bg-white p-6 shadow-xl"><button aria-label="Close" onClick={onClose} className="float-right"><X className="w-5"/></button>{children}</div></div>}
const newLine=():PurchaseLine=>({id:id(),description:'',quantity:1,rate:0,gstRate:18,account:'Purchases'}); const newVoucherLine=(entryType:'debit'|'credit'='debit',account=''):VoucherLine=>({id:id(),account,entryType,debit:0,credit:0,description:''});
export function PurchaseInvoicePage(){const {data,setData}=useAccounting();const [editing,setEditing]=useState<PurchaseInvoice|null>(null);const [error,setError]=useState('');const fresh=():PurchaseInvoice=>({id:id(),number:`PI-${String(data.purchaseInvoices.length+1).padStart(4,'0')}`,vendor:'',gstin:'',vendorInvoiceNumber:'',invoiceDate:today(),dueDate:today(),taxMode:'intra',notes:'',lines:[newLine()],status:'draft'});const save=(post=false)=>{if(!editing)return;const err=validateInvoice(data,editing);if(post&&err){setError(err);return}const row={...editing,status:post?'posted':editing.status};setData({...data,purchaseInvoices:data.purchaseInvoices.some(x=>x.id===row.id)?data.purchaseInvoices.map(x=>x.id===row.id?row:x):[row,...data.purchaseInvoices]});setEditing(null)};return <div className="space-y-6"><Header title="Purchase Invoice" description="Record vendor bills and post payable expenses." action={<button className={button} onClick={()=>{setError('');setEditing(fresh())}}><Plus className="w-4"/>New Purchase Invoice</button>}/><Table headers={['Invoice','Vendor','Date','Due','Total','Paid','Outstanding','Payment','Status','Actions']}><>{data.purchaseInvoices.map(x=><tr key={x.id}><td>{x.vendorInvoiceNumber}</td><td>{x.vendor}</td><td>{x.invoiceDate}</td><td>{x.dueDate}</td><td>{money(invoiceTotals(x).total)}</td><td>{money(invoiceTotals(x).total-outstanding(data,x))}</td><td>{money(outstanding(data,x))}</td><td><Status value={paymentStatus(data,x)}/></td><td><Status value={x.status}/></td><td className="space-x-1">{x.status==='draft'&&<><button className={secondary} onClick={()=>{setError('');setEditing(x)}}>Edit</button><button className={secondary} onClick={()=>{setEditing(x);setTimeout(()=>{},0)}}>Post</button><button className={secondary} onClick={()=>setData({...data,purchaseInvoices:data.purchaseInvoices.filter(i=>i.id!==x.id)})}>Delete</button></>}{x.status==='posted'&&<button className={secondary} onClick={()=>setData({...data,purchaseInvoices:data.purchaseInvoices.map(i=>i.id===x.id?{...i,status:'cancelled'}:i)})}>Cancel</button>}</td></tr>)}</></Table>{!data.purchaseInvoices.length&&<Empty text="Create a purchase invoice to track vendor bills and payables."/>}{editing&&<Modal onClose={()=>setEditing(null)}><InvoiceForm value={editing} setValue={setEditing}/>{error&&<p className="mt-3 text-sm text-red-600">{error}</p>}<div className="mt-5 flex gap-2"><button className={secondary} onClick={()=>save(false)}>Save draft</button><button className={button} onClick={()=>save(true)}>Post invoice</button></div></Modal>}</div>}
function InvoiceForm({value,setValue}:{value:PurchaseInvoice;setValue:(x:PurchaseInvoice)=>void}){const t=invoiceTotals(value);const set=(k:keyof PurchaseInvoice,v:string)=>setValue({...value,[k]:v});const line=(index:number,k:keyof PurchaseLine,v:string)=>{const lines=value.lines.map((l,i)=>i===index?{...l,[k]:['quantity','rate','gstRate'].includes(k)?Number(v):v}:l);setValue({...value,lines})};return <div><h2 className="text-2xl font-serif font-bold">Purchase invoice</h2><div className="mt-4 grid grid-cols-1 gap-3 md:grid-cols-3"><Field label="Purchase invoice number"><input className={input} value={value.number} onChange={e=>set('number',e.target.value)}/></Field><Field label="Vendor"><input className={input} value={value.vendor} onChange={e=>set('vendor',e.target.value)}/></Field><Field label="Vendor GSTIN"><input className={input} value={value.gstin} onChange={e=>set('gstin',e.target.value)}/></Field><Field label="Supplier invoice number"><input className={input} value={value.vendorInvoiceNumber} onChange={e=>set('vendorInvoiceNumber',e.target.value)}/></Field><Field label="Invoice date"><input type="date" className={input} value={value.invoiceDate} onChange={e=>set('invoiceDate',e.target.value)}/></Field><Field label="Due date"><input type="date" className={input} value={value.dueDate} onChange={e=>set('dueDate',e.target.value)}/></Field><Field label="Tax mode"><select className={input} value={value.taxMode} onChange={e=>set('taxMode',e.target.value)}><option value="intra">Intra-state (CGST + SGST)</option><option value="inter">Inter-state (IGST)</option><option value="none">No GST</option></select></Field></div><div className="mt-5 overflow-auto"><table className="w-full text-sm"><thead><tr><th>Description</th><th>Account</th><th>Qty</th><th>Rate</th><th>GST %</th><th/></tr></thead><tbody>{value.lines.map((l,i)=><tr key={l.id}><td><input className={input} value={l.description} onChange={e=>line(i,'description',e.target.value)}/></td><td><select className={input} value={l.account} onChange={e=>line(i,'account',e.target.value)}>{CHART.filter(a=>a.type==='Expense').map(a=><option key={a.name}>{a.name}</option>)}</select></td><td><input type="number" className={input} value={l.quantity} onChange={e=>line(i,'quantity',e.target.value)}/></td><td><input type="number" className={input} value={l.rate} onChange={e=>line(i,'rate',e.target.value)}/></td><td><input type="number" className={input} value={l.gstRate} onChange={e=>line(i,'gstRate',e.target.value)}/></td><td><button className={secondary} onClick={()=>setValue({...value,lines:value.lines.filter((_,n)=>n!==i)})}>Remove</button></td></tr>)}</tbody></table></div><button className={`mt-2 ${secondary}`} onClick={()=>setValue({...value,lines:[...value.lines,newLine()]})}>Add line</button><Field label="Notes / narration"><textarea className={input} value={value.notes} onChange={e=>set('notes',e.target.value)}/></Field><div className="mt-3 text-sm font-medium">Taxable {money(t.taxable)} · CGST {money(t.cgst)} · SGST {money(t.sgst)} · IGST {money(t.igst)} · Total GST {money(t.gst)} · Grand total {money(t.total)}</div></div>}
export function BankPaymentsPage(){const {data,setData}=useAccounting();const [editing,setEditing]=useState<BankPayment|null>(null);const [error,setError]=useState('');const fresh=():BankPayment=>({id:id(),number:`BP-${String(data.bankPayments.length+1).padStart(4,'0')}`,date:today(),payee:'',bankAccount:'Main Bank',category:'Rent Expense',linkedInvoiceId:'',mode:'Bank transfer',reference:'',amount:0,narration:'',clearance:'Pending',status:'draft'});const save=(post:boolean)=>{if(!editing)return;const inv=data.purchaseInvoices.find(i=>i.id===editing.linkedInvoiceId);if(!editing.payee||editing.amount<=0)return setError('Payee and a positive amount are required.');if(inv&&editing.amount>outstanding(data,inv)+.005)return setError('Payment exceeds invoice outstanding amount.');const x={...editing,status:post?'posted':editing.status};setData({...data,bankPayments:data.bankPayments.some(p=>p.id===x.id)?data.bankPayments.map(p=>p.id===x.id?x:p):[x,...data.bankPayments]});setEditing(null)};return <div className="space-y-6"><Header title="Bank Payments" description="Record cash and bank payments, including purchase invoice settlements." action={<button className={button} onClick={()=>{setError('');setEditing(fresh())}}><Plus className="w-4"/>New Payment</button>}/><Table headers={['Number','Date','Payee','Account','Category','Invoice','Mode','Amount','Clearance','Status','Actions']}><>{data.bankPayments.map(p=><tr key={p.id}><td>{p.number}</td><td>{p.date}</td><td>{p.payee}</td><td>{p.bankAccount}</td><td>{p.category}</td><td>{data.purchaseInvoices.find(i=>i.id===p.linkedInvoiceId)?.vendorInvoiceNumber||'—'}</td><td>{p.mode}</td><td>{money(p.amount)}</td><td><Status value={p.clearance}/></td><td><Status value={p.status}/></td><td>{p.status==='draft'&&<><button className={secondary} onClick={()=>setEditing(p)}>Edit</button><button className={secondary} onClick={()=>setData({...data,bankPayments:data.bankPayments.filter(x=>x.id!==p.id)})}>Delete</button></>}{p.status==='posted'&&<button className={secondary} onClick={()=>setData({...data,bankPayments:data.bankPayments.map(x=>x.id===p.id?{...x,status:'cancelled'}:x)})}>Cancel</button>}</td></tr>)}</></Table>{!data.bankPayments.length&&<Empty text="Record a payment to update cash, bank, and payable balances."/>}{editing&&<Modal onClose={()=>setEditing(null)}><h2 className="text-2xl font-serif font-bold">{editing.number}</h2><div className="mt-4 grid grid-cols-1 gap-3 md:grid-cols-3"><Field label="Payment date"><input type="date" className={input} value={editing.date} onChange={e=>setEditing({...editing,date:e.target.value})}/></Field><Field label="Payee"><input className={input} value={editing.payee} onChange={e=>setEditing({...editing,payee:e.target.value})}/></Field><Field label="Bank account"><select className={input} value={editing.bankAccount} onChange={e=>setEditing({...editing,bankAccount:e.target.value as 'Cash'|'Main Bank'})}><option>Cash</option><option>Main Bank</option></select></Field><Field label="Purpose / category"><select className={input} value={editing.category} onChange={e=>setEditing({...editing,category:e.target.value})}>{CHART.filter(a=>a.type==='Expense'||a.name==='Vendor Advances'||a.name==='Accounts Payable').map(a=><option key={a.name}>{a.name}</option>)}</select></Field><Field label="Linked purchase invoice"><select className={input} value={editing.linkedInvoiceId} onChange={e=>setEditing({...editing,linkedInvoiceId:e.target.value})}><option value="">None</option>{data.purchaseInvoices.filter(i=>i.status==='posted'&&outstanding(data,i)>0).map(i=><option key={i.id} value={i.id}>{i.vendorInvoiceNumber} · {i.vendor} · {money(outstanding(data,i))}</option>)}</select></Field><Field label="Mode"><select className={input} value={editing.mode} onChange={e=>setEditing({...editing,mode:e.target.value})}>{['Bank transfer','UPI','Cheque','Cash','Card'].map(x=><option key={x}>{x}</option>)}</select></Field><Field label="Reference"><input className={input} value={editing.reference} onChange={e=>setEditing({...editing,reference:e.target.value})}/></Field><Field label="Amount"><input type="number" className={input} value={editing.amount||''} onChange={e=>setEditing({...editing,amount:Number(e.target.value)})}/></Field><Field label="Clearance"><select className={input} value={editing.clearance} onChange={e=>setEditing({...editing,clearance:e.target.value as 'Pending'|'Cleared'})}><option>Pending</option><option>Cleared</option></select></Field></div><Field label="Narration"><textarea className={input} value={editing.narration} onChange={e=>setEditing({...editing,narration:e.target.value})}/></Field>{error&&<p className="mt-2 text-sm text-red-600">{error}</p>}<div className="mt-4 flex gap-2"><button className={secondary} onClick={()=>save(false)}>Save draft</button><button className={button} onClick={()=>save(true)}>Post payment</button></div></Modal>}</div>}

export function LedgerPage(){const {data}=useAccounting();const [account,setAccount]=useState('');const [start,setStart]=useState('');const [end,setEnd]=useState('9999-12-31');const [search,setSearch]=useState('');const rows=useMemo(()=>{let r=ledgerEntries(data).filter(x=>(!account||x.account===account)&&x.date>=start&&x.date<=end&&(!search||`${x.reference} ${x.party} ${x.narration}`.toLowerCase().includes(search.toLowerCase())));let run=0;return r.map(x=>({...x,balance:(run+=x.debit-x.credit)}))},[data,account,start,end,search]);const debit=rows.reduce((s,x)=>s+x.debit,0),credit=rows.reduce((s,x)=>s+x.credit,0);return <div className="space-y-6"><Header title="Ledger" description="Trace posted transactions by account." action={<button className={button} disabled={!rows.length} onClick={()=>exportCsv('ledger.csv',['Date','Source','Reference','Party','Narration','Debit','Credit','Balance'],rows.map(x=>[x.date,x.source,x.reference,x.party,x.narration,x.debit,x.credit,x.balance]))}><Download className="w-4"/>Export CSV</button>}/><div className="grid gap-3 md:grid-cols-4"><select className={input} value={account} onChange={e=>setAccount(e.target.value)}><option value="">All accounts</option>{CHART.map(a=><option key={a.name}>{a.name}</option>)}</select><input type="date" className={input} value={start} onChange={e=>setStart(e.target.value)}/><input type="date" className={input} value={end==='9999-12-31'?'':end} onChange={e=>setEnd(e.target.value||'9999-12-31')}/><input className={input} placeholder="Search reference, party..." value={search} onChange={e=>setSearch(e.target.value)}/></div><div className="text-sm">Total debit {money(debit)} · Total credit {money(credit)} · Closing balance {money(debit-credit)}</div><Table headers={['Date','Source','Reference','Party','Narration','Debit','Credit','Running balance']}><>{rows.map((x,i)=><tr key={i}><td>{x.date}</td><td>{x.source}</td><td>{x.reference}</td><td>{x.party}</td><td>{x.narration}</td><td>{money(x.debit)}</td><td>{money(x.credit)}</td><td>{money(x.balance)}</td></tr>)}</></Table>{!rows.length&&<Empty text="Post an invoice, voucher, or payment to see ledger entries."/>}</div>}

export function ProfitLossPage(){const {data}=useAccounting();const [start,setStart]=useState('');const [end,setEnd]=useState('9999-12-31');const p=profitLoss(data,start,end);return <div className="space-y-6"><Header title="Profit & Loss" description="Income and expense statement from posted transactions." action={<button className={button} onClick={()=>exportCsv('profit-loss.csv',['Account','Type','Amount'],p.rows.map(r=>[r.account,r.type,r.value]))}><Download className="w-4"/>Export CSV</button>}/><div className="flex gap-3"><input type="date" className={input} value={start} onChange={e=>setStart(e.target.value)}/><input type="date" className={input} value={end==='9999-12-31'?'':end} onChange={e=>setEnd(e.target.value||'9999-12-31')}/></div><div className="grid gap-4 md:grid-cols-3"><Card title="Total income" value={money(p.income)}/><Card title="Total expenses" value={money(p.expenses)}/><Card title={p.net>=0?'Net profit':'Net loss'} value={money(p.net)}/></div><Table headers={['Account','Type','Amount']}><>{p.rows.map(r=><tr key={r.account}><td>{r.account}</td><td>{r.type}</td><td>{money(r.value)}</td></tr>)}</></Table>{!p.rows.length&&<Empty text="Post income or expense transactions to generate P & L."/>}</div>}

export function ReportsPage(){const {data}=useAccounting();const [tab,setTab]=useState('Trial Balance');const [asOf,setAsOf]=useState(today());const tb=trialBalance(data,asOf);const purchases=data.purchaseInvoices.filter(i=>i.status==='posted'&&i.invoiceDate<=asOf);const pays=data.bankPayments.filter(p=>p.status==='posted'&&p.date<=asOf);const rows=tab==='Trial Balance'?tb:tab==='Purchase Register'?purchases:tab==='Bank Payment Register'?pays:tab==='Accounts Payable'?purchases.filter(i=>outstanding(data,i)>0):tb;const headers=tab==='Trial Balance'?['Account','Debit','Credit']:tab==='Purchase Register'?['Invoice','Vendor','Date','Due','Taxable','GST','Total','Paid','Outstanding']:tab==='Bank Payment Register'?['Payment','Date','Payee','Account','Mode','Reference','Amount','Clearance']:tab==='Accounts Payable'?['Vendor','Invoice','Date','Due','Total','Paid','Outstanding','Days overdue']:['Account','Type','Debit','Credit','Closing'];const csv=rows.map((r:any)=>tab==='Trial Balance'?[r.account,r.debit,r.credit]:tab==='Purchase Register'?[r.vendorInvoiceNumber,r.vendor,r.invoiceDate,r.dueDate,invoiceTotals(r).taxable,invoiceTotals(r).gst,invoiceTotals(r).total,invoiceTotals(r).total-outstanding(data,r),outstanding(data,r)]:tab==='Bank Payment Register'?[r.number,r.date,r.payee,r.bankAccount,r.mode,r.reference,r.amount,r.clearance]:tab==='Accounts Payable'?[r.vendor,r.vendorInvoiceNumber,r.invoiceDate,r.dueDate,invoiceTotals(r).total,invoiceTotals(r).total-outstanding(data,r),outstanding(data,r),Math.max(0,Math.floor((Date.parse(asOf)-Date.parse(r.dueDate))/86400000))]:[r.account,r.type,r.debit,r.credit,r.debit-r.credit]);return <div className="space-y-6"><Header title="Reports" description="Registers, balances, and statutory-ready summaries." action={<button className={button} onClick={()=>exportCsv(`${tab.replaceAll(' ','-').toLowerCase()}.csv`,headers,csv)}><Download className="w-4"/>Export CSV</button>}/><div className="flex flex-wrap gap-2">{['Trial Balance','Purchase Register','Bank Payment Register','Accounts Payable','Account Balances'].map(x=><button key={x} className={x===tab?button:secondary} onClick={()=>setTab(x)}>{x}</button>)}<input type="date" className={`${input} ml-auto max-w-44`} value={asOf} onChange={e=>setAsOf(e.target.value)}/></div><Table headers={headers}><>{rows.map((r:any,i)=><tr key={i}>{csv[i].map((v:any,j:number)=><td key={j}>{typeof v==='number'?money(v):v}</td>)}</tr>)}</></Table>{!rows.length&&<Empty text="Post accounting transactions to populate reports."/>}</div>}

function Table({headers,children}:{headers:string[];children:React.ReactNode}){return <div className="overflow-auto rounded-lg border border-[#EFECE5] bg-white"><table className="min-w-full text-left text-sm"><thead className="bg-[#F7F5F0] text-xs uppercase text-gray-500"><tr>{headers.map(h=><th className="whitespace-nowrap px-3 py-2" key={h}>{h}</th>)}</tr></thead><tbody className="divide-y divide-gray-100"><>{children}</></tbody></table></div>}
function Empty({text}:{text:string}){return <div className="rounded-lg border border-dashed border-gray-300 bg-white p-10 text-center text-sm text-gray-600">{text}</div>}
function Field({label,children}:{label:string;children:React.ReactNode}){return <label className="mt-3 block text-sm font-medium text-gray-700">{label}{children}</label>}
function Card({title,value}:{title:string;value:string}){return <div className="rounded-lg border border-[#EFECE5] bg-white p-4"><div className="text-xs uppercase text-gray-500">{title}</div><div className="mt-1 text-xl font-semibold">{value}</div></div>}
